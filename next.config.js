/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  /* experimental: {
    images: {
      unoptimized: false,
    },
  }, */
};

module.exports = nextConfig;
